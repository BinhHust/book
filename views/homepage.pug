extends base

block content 
  .container
    .row.my-3
      each shop in shops
        .col-4
          .border.border-dark.p-3
            h2 #{shop.name}
            p #{shop.description}
            p Quantity: #{shop.nBooks} books
            a.btn.btn-info(href=`/shop/${shop.slug}`) Detail shop
            